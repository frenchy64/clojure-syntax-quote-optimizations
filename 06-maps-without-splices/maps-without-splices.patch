From 321e6df5a36cf057ecadd60f3a8180dab4a563c3 Mon Sep 17 00:00:00 2001
From: GitHub Copilot <copilot@github.com>
Date: Wed, 8 Oct 2025 06:49:08 +0000
Subject: [PATCH] Optimize syntax-quote for maps without splices

---
 src/jvm/clojure/lang/LispReader.java | 21 ++++++++++++++++++++-
 1 file changed, 20 insertions(+), 1 deletion(-)

diff --git a/src/jvm/clojure/lang/LispReader.java b/src/jvm/clojure/lang/LispReader.java
index 8d7079cf..a9df7ae1 100644
--- a/src/jvm/clojure/lang/LispReader.java
+++ b/src/jvm/clojure/lang/LispReader.java
@@ -1093,7 +1093,14 @@ public static class SyntaxQuoteReader extends AFn{
 			else if(form instanceof IPersistentMap)
 				{
 				IPersistentVector keyvals = flattenMap(form);
-				ret = RT.list(APPLY, HASHMAP, RT.list(SEQ, RT.cons(CONCAT, sqExpandList(keyvals.seq()))));
+				ISeq seq = keyvals.seq();
+				// `{~@k ~@v} => (apply hash-map (concat k v))
+				if(hasSplice(seq))
+					ret = RT.list(APPLY, HASHMAP, RT.cons(CONCAT, sqExpandList(seq)));
+				// `{k v} => {`k `v}
+				else
+				// `{k v ...} => {`k `v ...}
+					ret = PersistentArrayMap.createAsIfByAssoc(RT.toArray(sqExpandList(seq)));
 				}
 			else if(form instanceof IPersistentVector)
 				{
@@ -1147,6 +1154,18 @@ public static class SyntaxQuoteReader extends AFn{
 		return ret.seq();
 	}
 
+
+	// returns true iff seq contains ~@
+	private static boolean hasSplice(ISeq seq) {
+		for(; seq != null; seq = seq.next())
+			{
+			if(isUnquoteSplicing(seq.first()))
+				return true;
+			}
+		return false;
+	}
+
+
 	private static IPersistentVector flattenMap(Object form){
 		IPersistentVector keyvals = PersistentVector.EMPTY;
 		for(ISeq s = RT.seq(form); s != null; s = s.next())
-- 
2.51.0

