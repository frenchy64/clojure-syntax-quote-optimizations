From 3bd8c22e2445f5ef50c38319ca7901f3658fd75b Mon Sep 17 00:00:00 2001
From: GitHub Copilot <copilot@github.com>
Date: Wed, 8 Oct 2025 07:01:00 +0000
Subject: [PATCH] Optimize syntax-quote for singleton sets

Optimize syntax-quoted sets with exactly one element without splices
to use set literals instead of (apply hash-set ...).

For example: `#{:a} => #{:a} instead of (apply hash-set ...)

---
 src/jvm/clojure/lang/LispReader.java | 19 ++++++++++++++++++-
 1 file changed, 18 insertions(+), 1 deletion(-)

diff --git a/src/jvm/clojure/lang/LispReader.java b/src/jvm/clojure/lang/LispReader.java
index 8d7079cf..4f54829a 100644
--- a/src/jvm/clojure/lang/LispReader.java
+++ b/src/jvm/clojure/lang/LispReader.java
@@ -1101,7 +1101,14 @@ public static class SyntaxQuoteReader extends AFn{
 				}
 			else if(form instanceof IPersistentSet)
 				{
-				ret = RT.list(APPLY, HASHSET, RT.list(SEQ, RT.cons(CONCAT, sqExpandList(((IPersistentSet) form).seq()))));
+				ISeq seq = ((IPersistentSet) form).seq();
+				// `#{a} => #{`a}
+				if(seq != null && seq.count() == 1 && !hasSplice(seq))
+					ret = PersistentHashSet.create(RT.toArray(syntaxQuote(seq.first())));
+				// `#{~@a ...} => (apply hash-set (seq (concat a ...)))
+				else
+					ret = RT.list(APPLY, HASHSET, RT.list(SEQ, RT.cons(CONCAT, sqExpandList(seq))));
+				}
 				}
 			else if(form instanceof ISeq || form instanceof IPersistentList)
 				{
@@ -1147,6 +1154,16 @@ public static class SyntaxQuoteReader extends AFn{
 		return ret.seq();
 	}
 
+	// returns true iff seq contains ~@
+	private static boolean hasSplice(ISeq seq) {
+		for(; seq != null; seq = seq.next())
+			{
+			if(isUnquoteSplicing(seq.first()))
+				return true;
+			}
+		return false;
+	}
+
 	private static IPersistentVector flattenMap(Object form){
 		IPersistentVector keyvals = PersistentVector.EMPTY;
 		for(ISeq s = RT.seq(form); s != null; s = s.next())
-- 
2.51.0

